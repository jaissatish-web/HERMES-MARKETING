'use client';
import { useRouter } from 'next/navigation';
export default function BudgetsPage(){const r=useRouter();const nav=[['⌂','Overview','/dashboard'],['▦','Services','/dashboard/services'],['◈','Providers & Models','/dashboard/providers'],['◇','Credentials','/dashboard/credentials'],['✓','Approvals','/dashboard/approvals'],['◷','Budgets','/dashboard/budgets'],['↗','Jobs','/dashboard/jobs'],['≡','Audit','/dashboard/audit']];return <main className="appShell"><aside className="sidebar"><div className="brand"><span>GH</span><div><strong>GATE HUB</strong><small>GCC-MENTOR</small></div></div><div className="workspace"><span className="workspaceDot"/> Founder workspace <b>ADMIN</b></div><nav>{nav.map(([i,n,p])=><button key={n} className={n==='Budgets'?'active':''} onClick={()=>r.push(p)}><span>{i}</span>{n}</button>)}</nav></aside><section className="mainArea"><header className="topbar"><div className="crumb"><span>GATE HUB</span><b>/</b> Budget Manager</div><span className="livePill"><i/> COST CONTROL</span></header><div className="page"><div className="pageIntro"><div><p className="eyebrow">COST CONTROL</p><h1>Budgets</h1><p className="muted">Set spending limits so AI services cannot silently exceed your plan.</p></div><button className="primary compactButton">+ Add budget</button></div><section className="metricGrid"><div className="metricCard featured"><span>Monthly budget</span><strong>$0</strong><p>Not configured yet</p></div><div className="metricCard"><span>Spent</span><strong>$0</strong><p>This period</p></div><div className="metricCard"><span>Remaining</span><strong>$0</strong><p>After limits are set</p></div><div className="metricCard"><span>Alerts</span><strong>0</strong><p>No thresholds configured</p></div></section><section className="featureGrid"><article className="featureCard dark"><div className="featureIcon">$</div><span className="tag">LIMITS</span><h3>Control AI spend</h3><p>Set monthly or per-service limits for research, content, image and video work.</p></article><article className="featureCard"><div className="featureIcon light">!</div><span className="tag">ALERTS</span><h3>Know before you exceed</h3><p>Warning thresholds can notify you before a service reaches its limit.</p></article><article className="featureCard"><div className="featureIcon light">✓</div><span className="tag">SAFETY</span><h3>Stop when needed</h3><p>Execution rules can pause a service when its approved budget is exhausted.</p></article></section><section className="emptyService"><div className="emptyIcon">◷</div><h2>No budgets configured</h2><p>Budget rules will be wired to the Service Gateway after services are connected.</p></section></div></section></main>}
