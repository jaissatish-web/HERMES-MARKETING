'use client';
import { useRouter } from 'next/navigation';
export default function JobsPage(){const r=useRouter();const nav=[['⌂','Overview','/dashboard'],['▦','Services','/dashboard/services'],['◈','Providers & Models','/dashboard/providers'],['◇','Credentials','/dashboard/credentials'],['✓','Approvals','/dashboard/approvals'],['◷','Budgets','/dashboard/budgets'],['↗','Jobs','/dashboard/jobs'],['≡','Audit','/dashboard/audit']];return <main className="appShell"><aside className="sidebar"><div className="brand"><span>GH</span><div><strong>GATE HUB</strong><small>GCC-MENTOR</small></div></div><div className="workspace"><span className="workspaceDot"/> Founder workspace <b>ADMIN</b></div><nav>{nav.map(([i,n,p])=><button key={n} className={n==='Jobs'?'active':''} onClick={()=>r.push(p)}><span>{i}</span>{n}</button>)}</nav></aside><section className="mainArea"><header className="topbar"><div className="crumb"><span>GATE HUB</span><b>/</b> Job Monitor</div><span className="livePill"><i/> EXECUTION</span></header><div className="page"><div className="pageIntro"><div><p className="eyebrow">EXECUTION LAYER</p><h1>Jobs</h1><p className="muted">See what GATE HUB is running, waiting for approval or has completed.</p></div></div><section className="metricGrid"><div className="metricCard featured"><span>Running</span><strong>0</strong><p>Active jobs</p></div><div className="metricCard"><span>Waiting</span><strong>0</strong><p>Queued jobs</p></div><div className="metricCard"><span>Completed</span><strong>0</strong><p>No executions yet</p></div><div className="metricCard"><span>Failed</span><strong>0</strong><p>No failures recorded</p></div></section><section className="featureGrid"><article className="featureCard dark"><div className="featureIcon">↗</div><span className="tag">LIVE</span><h3>Execution status</h3><p>Jobs will show provider, model, service, duration and result once the Service Gateway is active.</p></article><article className="featureCard"><div className="featureIcon light">✓</div><span className="tag">QUEUE</span><h3>Approval-aware</h3><p>Jobs that require Founder approval will wait here instead of executing automatically.</p></article><article className="featureCard"><div className="featureIcon light">$</div><span className="tag">COST</span><h3>Usage captured</h3><p>Execution records will include usage and cost so the Budget Manager can enforce limits.</p></article></section><section className="emptyService"><div className="emptyIcon">↗</div><h2>No jobs yet</h2><p>The execution queue will activate after the Service Gateway is built.</p></section></div></section></main>}
